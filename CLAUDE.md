# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

"Basic Crawler" — a NetHack/Rogue-inspired roguelike dungeon crawler built in **Godot 4.6.3** (GDScript). Randomly generated dungeons, turn-based OSR/Basic-Fantasy combat, permadeath. The full design brief is in `Basic Crawler Game Plan.md`. Targets desktop, mobile, and web via the **GL Compatibility** renderer — do not switch renderers, and don't add features that break web/mobile export.

## Environment

This is a **Windows** development machine. Use **PowerShell** syntax for shell commands unless told otherwise (`$null` not `/dev/null`, `$env:VAR` not `$VAR`, backtick for line continuation; cmdlets like `Get-Content`/`Select-String` over `cat`/`grep`). Paths are Windows-style (`C:\Users\itaiag\...`). The validation commands below are written for PowerShell.

## Workflow expectation (important)

Work is delivered in **small, reviewable stages, and within a stage in incremental steps**; the owner checks each step (usually by running the editor and sending a screenshot) before the next. Do not jump ahead. When a stage is broad, confirm scope/order with the owner first (use the question tool), then implement one reviewable step at a time. The owner reviews visual changes by screenshot — many visual claims cannot be self-verified headless; say so rather than asserting success.

Status: Stages 1-3 (dungeon + FOV + doors; monsters + bump combat + chase AI + death; ability scores + character creation) are done. Stage 4 is largely in place — gold/potions/quaff, weapons/armor with two-handed-vs-shield rule, rest/sleep with fatigue penalty applied to attack rolls, close-door, wield/wear, monster morale (2d6) with fleeing, blocking **pillars** (real gameplay feature), battle-zoom camera effect, side-docked inventory panel, atmosphere layer (room moods + wall cracks + floor details), a **lighting/atmosphere polish** layer (per-cell torchlight falloff with flame flicker, screen vignette, wall bevels, token halo/shadows) — all faked with circles and a radial `GradientTexture2D`, **no shaders**, to stay GL-compatibility / web / mobile safe, **kick-monster brawling** (Basic Fantasy rules: `k` + direction, d20 + melee_bonus - 2 vs AC, 1d4 damage; metal-armored targets rebound damage to soft-armored kickers via `"metal_armor": true` in monster data — none of the current monsters have this), and **ranged attacks** (`f` to fire a wielded launcher down a cardinal line: bows→arrows, crossbows→bolts, slings→stones; matched-launcher rule, ammo carries the damage die, DEX-to-hit, finite ammo with 50%-on-miss recovery — see the firing flow below), and **multi-level dungeons** (`>`/`<` to descend/ascend on stairs; visited levels are **persisted** so revisiting a level restores it exactly — killed monsters stay dead, looted items gone, fog kept; deeper levels are gently busier; current depth shown as `DLvl:N` on the status bar — see the multi-level model below). **Not yet built**: traps & searching. Per the design doc, attack/saving-throw rolls are shown to the player; trap/secret-door **search rolls must stay hidden**.

## Validating changes

There is no unit-test suite. There are three layers of headless validation; use the right one for the change.

1. **Boot check** — compiles every script and runs `Game._ready()`:
   ```
   "C:\Users\itaiag\Desktop\godot\Godot_v4.6.3-stable_win64_console.exe" --headless --path "C:\Users\itaiag\git\godot\basic-crawler" --quit-after 30
   ```
   Exit 0 with no `ERROR`/`SCRIPT` lines = scripts parsed and `_ready` ran. **Headless does not simulate input or combat** — attack rolls, monster turns, overlays, the inventory panel build, etc. never execute, so runtime errors there slip through. After renaming/removing a field or method, **grep every call site** rather than relying on this check.

2. **Gameplay smoke test** (`tests/smoke_test.gd`) — a persistent `extends SceneTree` script that instances `game.tscn`, waits a frame for `_ready`, then drives one move and one bump-attack through the real code (`_try_move` → `_run_round` → `_do_move_action`/`_attack_monster`). Re-run after touching turn/combat/input/movement code:
   ```
   "C:\Users\itaiag\Desktop\godot\Godot_v4.6.3-stable_win64_console.exe" --headless --path "C:\Users\itaiag\git\godot\basic-crawler" --script res://tests/smoke_test.gd
   ```
   Pass = exit 0 + `SMOKE TEST PASSED` + no `SCRIPT ERROR` lines. It's dev-only (never referenced by `game.tscn`).

3. **Throwaway logic checks** — for anything the smoke test doesn't cover (stat math, morale/fleeing transitions, pillar generation constraints, zoom state transitions, atmosphere data invariants), write a short `extends SceneTree` script in `scripts/_tmp_<x>.gd` with an `_init()` that constructs the relevant objects (or instances the scene + `await process_frame`) and asserts results. Print `PASSED`/`FAILED` and `quit(0/1)`. **Delete the script and its `.uid`** after the run.

**Adding a new `class_name` requires registering it** before a plain `--script` headless run can resolve the name. Run an editor pass once: `--headless --editor --quit --path <project>` (watch for `update_scripts_classes | <YourClass>`), then validate normally.

## Architecture

**No autoloads.** Shared code is exposed via `class_name`: `GameData`, `DungeonGenerator`, `FOV`, `Monster`, plus the extracted pure-logic helpers `TextFmt`, `MonsterAI`, `DungeonPopulator` (all `RefCounted`, static methods only). Three runtime-built nodes deliberately have **no** `class_name` and are referenced by node var / `load()` instead — `light_overlay.gd`, `combat_panel.gd`, and `projectile.gd` (the transient ranged-shot streak) — so they need no editor registration pass. The runnable scene is `scenes/game.tscn`; `scripts/game.gd` is the root controller and still owns most gameplay state and the turn loop, but the self-contained clusters have been split into their own files (combat panel, monster AI, dungeon population, text helpers — see below).

Scene tree: `Game (Node2D, game.gd)` → `DungeonRenderer (Node2D)`, `Player (Node2D, player.gd)` → `Camera (Camera2D)`, `UI (CanvasLayer)` → `MessageLog`, `StatusBar` (both `RichTextLabel`). The full-screen character-creation overlay and the right-docked side panel (currently holding the inventory) are created at runtime as children of `UI`. Monsters are created at runtime as children of `Game`. Three more nodes are built at runtime in `_ready`: the **torchlight overlay** (`_light`, `light_overlay.gd`, child of `Game`, `z_index = 10` so it darkens on top of tiles and tokens), a screen-space **vignette** `TextureRect` (child of `UI`, moved to index 0 so it sits behind the text labels), and the **combat panel** (`_combat_panel`, `combat_panel.gd`, a controller node under `Game` that owns its own card `ColorRect`+`RichTextLabel` under `UI`).

- **`game_data.gd`** — single source of truth for data and pure helpers. `Tile` enum includes `PILLAR` (real blocking tile). Grid constants (`CELL`, `MAP_W=80`, `MAP_H=21`, `FONT_SIZE`, `FOV_RADIUS`). `MonsterKind` + `MONSTERS` stat table (each row carries `morale`). `ItemKind` + `ITEMS` table (potion/weapon/armor/shield, plus **ranged** launchers and **ammo** — a launcher carries `ammo_type`+`range`+a weak melee bash die, ammo carries the `dmg_n/dmg_d` shot dice; bow↔arrow, crossbow↔bolt, sling↔stone; predicates `is_weapon`/`is_armor`/`is_shield`/`is_potion`/`is_two_handed`/`is_ranged`/`is_ammo` and `ammo_type(kind)`, plus tile predicates `is_wall`/`is_pillar`/`is_door`). Tile predicates `is_passable`/`is_transparent` are **whitelists** — `PILLAR` is intentionally absent from both. Both stair tiles are passable+transparent; `get_tile_color` gives them distinct colours (`COLOR_STAIRS_DOWN` warm amber, `COLOR_STAIRS_UP` cool cyan) so `>` vs `<` read at a glance. `Mood` enum + `mood_color()` + `apply_mood(color, mood)` (10% lerp toward mood color; intentionally subtle). Dice helpers (`roll`, `roll_ability` = 4d6-drop-lowest) and Basic Fantasy `ability_mod`.

- **`dungeon_generator.gd`** — pure data, no nodes. Places **floor rects** (spaced `grow(3)` apart so wall rings never touch), builds a wall ring per room, connects rooms with a **minimum spanning tree**; corridors are carved by **BFS through empty void only** (never stepping on walls/floors/doors), punching one perpendicular door per end. Then adds *mess* (a few extra MST-edge connections and dead-end stubs). After stairs, places **pillars** (`_add_pillars` → symmetric patterns: central / 2-symmetric / 4-corner; never on stairs or adjacent to doors; only commits a pattern if a flood-fill confirms the room's floor stays connected). Finally builds the **atmosphere layer**: `room_moods: Array[int]` (parallel to `rooms`), `tile_room: Dictionary[Vector2i,int]` covering each room's floor *and* wall ring (renderer uses `tile_room_at(cell)`), and sparse `wall_cracks`/`floor_details` decorations. Generation order matters: every later step uses `get_tile == FLOOR` filters that auto-exclude stairs, pillars, etc. Up-stairs (`STAIRS_UP`) are placed at the **first room's** centre, down-stairs (`STAIRS_DOWN`) at the **last room's** centre; `get_start_pos()` returns the up-stairs cell and `get_down_stairs_pos()` the down-stairs cell (these are the arrival points for descending/ascending — see the multi-level model).

- **`fov.gd`** — static recursive shadowcasting (RogueBasin 8-octant). `game.gd._update_fov` overlays NetHack-style **lit rooms** on top (standing in a room reveals the whole room + ring). Side effect: inside a lit room you can see "behind" your own pillars — the shadowcast still blocks behind pillars when the player is *outside* the room.

- **`dungeon_renderer.gd`** — custom `_draw()` over the whole map, monospace `SystemFont` (`resources/mono_font.tres`); **not a TileMap**. Hot loop honors fog via `visible_cells`/`explored_cells`; `reveal_all` is the F6 debug override. Drawing rules:
  - **Floors**: flat stone rectangles, mood-tinted via `dungeon.tile_room_at`. Sparse `floor_details` (crack / stain / moss / rubble) overlay only on plain `FLOOR` cells (skipped for cells routed through item/door/stairs/pillar branches).
  - **Walls**: **filled cell blocks** in `COLOR_WALL.darkened(0.6)`, with a lit-from-above **bevel** (lighter top strip, darker bottom, nudged side strips) for a carved 3D read, plus a thin warm highlight on edges that face open space (`_is_open_space`); a sparse `wall_cracks` overlay sits on top. Walls inherit the room mood through the input color.
  - **Corridors**: two-pass connected bands — wider darker edge + narrower lighter core — to read as carved stone rather than highways. **Mood-neutral.**
  - **Doors**: closed/locked = thin wood slab spanning the passage (orientation derived from wall neighbors); open = floor opening with a small leaf hint against the jamb.
  - **Pillars**: capsule-shaped column with a lit top cap; body is `COLOR_PILLAR` (cool, distinct from warm walls), mood-tinted.
  - **`_dim(color)`**: desaturate 65% + darken 0.5 — applied to every explored-but-not-visible cell.
  - Holds an `items` dict reference (set once by `game.gd`); items render with a faint colored glow and remain after exploration.

- **`light_overlay.gd`** — `extends Node2D`, **no `class_name`** (referenced by node path, not type). A cheap second draw pass that pools torchlight around the player: for each cell in `visible_cells` it draws a black rect whose alpha lerps `SHADE_MIN`→`SHADE_MAX` by distance from `light_pos` over `radius`, multiplied by a `flicker` that breathes via two summed sines in `_process`. Redraws are throttled to `REDRAW_INTERVAL (1/15 s)` so the per-frame cost stays tiny — **only this overlay redraws on flicker; the heavy full-map tile pass in `dungeon_renderer.gd` is untouched.** `game.gd._update_fov` feeds it the player cell + visible set via `set_light`; `_toggle_reveal`/F6 sets `enabled = not reveal_all` so the debug map view isn't darkened.

- **`player.gd`** — `extends Node2D`, **no `class_name`** (so the loose-typing pattern below holds). Holds six ability scores, HP/XP/level, gold, `inventory`, `fatigued`, equipment slots `equipped_weapon/armor/shield` (`-1` = none). Combat values are **derived methods**, not stored:
  - `armor_class()` = `(equipped_armor ? ITEMS[armor].ac : BASE_AC=11) + (equipped_shield ? SHIELD_AC=1 : 0) + dex_mod()`
  - `weapon_dmg_n/d()` from the equipped weapon, else `UNARMED_DMG_N/D = 1/2`
  - `melee_attack_bonus()` = `level + str_mod()`, `damage_bonus()` = `str_mod()`, `ranged_attack_bonus()` = `level + dex_mod()` (missiles use DEX, no damage bonus). A wielded ranged launcher lives in the **same `equipped_weapon` slot**, so a bump-attack with a bow uses its weak bash die.
  - `gain_level_hp()` rolls `1dHIT_DIE(=8) + con_mod`
  Draws `@` centered on a dark token, under a faked **warm torch halo** (concentric falling-alpha circles, no texture) and a small grounding shadow; `grid_pos` is logical (set instantly), screen `position` is **tweened**; `place_at()` teleports and kills any in-flight tween. (`monster.gd` mirrors the grounding shadow but not the halo.)

- **`monster.gd`** — `class_name Monster`, mirrors the player node visually (letter glyph on a dark token, tweened `move_to`/`place_at`). Carries `kind` (index into `MONSTERS`), `hp`/`max_hp`, and two morale flags: `fleeing`, `morale_checked` (one-shot guard for the half-HP trigger).

- **`text_format.gd`** — `class_name TextFmt` (`RefCounted`, static). Pure shared text helpers: `cap(s)` (capitalize first letter) and `signed(n)` (" + 2" / " - 1" terms for roll breakdowns). Called from combat resolution, the combat panel, and the status bar.

- **`monster_ai.gd`** — `class_name MonsterAI` (`RefCounted`, static). Pure pathfinding: `chase_step` (greedy, longer axis first), `flee_step` (mirror of chase, steps away), `can_enter` (the `is_passable` + player-cell + occupancy test). The caller passes the dungeon, occupancy `Dictionary`, and player cell — nothing reads game state. `game.gd._monster_take_turn` drives these and still owns `_move_monster`/`_monsters_act` and the attacks.

- **`dungeon_populator.gd`** — `class_name DungeonPopulator` (`RefCounted`, static). Decides *what* goes *where*, returning plain data (never creates nodes): `roll_monsters(dungeon, occupancy, depth := 1) -> Array[Dictionary]` (`{kind, cell}`, with a local occupancy copy so two monsters never share a cell; `depth` gently raises the per-room count cap — `2 + (depth-1)/2` — the tuning hook for deeper-difficulty work), `roll_items(dungeon, occupancy, depth := 1) -> Dictionary` (`Vector2i -> item dict`; `depth` is threaded for future loot tuning but rates are flat for now; rolls gold/potion/weapon/armor plus a ranged launcher and an `ammo_item(kind, randi_range(5,15))` bundle — launchers and ammo spawn **independently**, so a found launcher may have no matching ammo and vice-versa, by design), `wandering_cell_near(...)`, `random_floor_cell`, and the item builders (`gold_item`/`potion_item`/`loot_item`/`ammo_item`, `random_{weapon,armor,ranged,ammo}_kind`). An `ammo_item` floor dict carries `ammo_count`; `game.gd._pickup_item` splits it into that many flat inventory entries. `game.gd._spawn_monsters` loops the result calling the kept `_add_monster` (node creation), and `_spawn_items` **merges in place** into `_items_at` (`merge(..., true)`) to preserve the renderer's by-reference link.

- **`combat_panel.gd`** — `extends Node`, **no `class_name`** (built at runtime via `load()`, like `light_overlay.gd`). The structured encounter card, lifted whole out of `game.gd`. Owns its own `ColorRect`+`RichTextLabel` under `UI` plus the panel constants, initiative/turn state, and the pure builders (`_header`, `_turn_section`, `_attack_block`, `_morale_block`, `_nat_note`, corner math). API: `setup(ui, font, viewport, top_h, bottom_h, area_provider)`, `begin_encounter`, `end_encounter`, `begin_turn`, `push_attack`, `push_morale`, `show_card`, `hide_card`. `game.gd` keeps `_combat_area_screen()` (needs the live camera transform + monster positions) and passes it as the `area_provider` closure so the panel can pick the farthest corner.

### Turn & combat model (central to `game.gd`)

- A **round** is `_run_round(player_action: Callable)`. Player input classifies the intent and passes a *callable* (`_do_move_action`/`_do_kick_action`/`_do_close_action`/`_do_wield_action`/`_do_wear_action`/`_do_quaff_action`) — the action is **re-derived at execution time** so it stays correct if monsters move first. `_run_round` finishes with `_update_fov / _update_status / _update_combat_camera_zoom`.
- **Kick (`_kick_monster`):** Basic Fantasy brawling. d20 + `melee_attack_bonus()` - `KICK_ATK_PENALTY (2)` - fatigue vs monster AC; on hit: 1d4, no damage bonus (subduing damage). Metal-armor blowback: if the player is in no armor or leather armor and the monster has `"metal_armor": true` in its `MONSTERS` entry, the 1d4 is applied to the player instead. No current monsters have `metal_armor: true`; the flag is a hook for future content.
- **Ranged fire (`_do_fire_action`):** the `f` flow re-derives at execution time (the launcher/ammo are re-validated, since monsters may have acted first). `_trace_shot(start, dir, max_range)` walks a straight cardinal line, returning `{monster, impact_cell, path}` — the first monster struck, else the last open cell on a block/range-cap, plus the open cells crossed (used for recovery). Attack = `d20 + ranged_attack_bonus() − fatigue` vs AC; damage = the ammo's dice (min 1, no STR). Uses the **same `_combat_panel.push_attack` / log format** as melee/kick ("You shoot/shoot at <monster>"). On a miss, `_try_recover_ammo` flips 50% and, if it wins, drops one missile as an `ammo_item(kind, 1)` on the first holdable cell scanning back from the impact toward the player (stacks via `ammo_count` if that cell already holds the same ammo). Sniping a monster outside `ENGAGE_RANGE (5)` logs to the message log but the combat panel only appears once `_engaged()` — intended. Each shot also spawns a transient cosmetic streak via `_spawn_projectile` (`projectile.gd`, `z_index 11` above the torchlight): a rotated line+head-dot tweened from the shooter to the impact cell, travel time `clampf(cells * 0.03, 0.08, 0.22)` (fast but notable), non-blocking — the turn has already resolved, and the streak frees itself on arrival (so it's harmless if the target was killed instantly).
- **Encounter-based group initiative (Basic Fantasy / OSR):** initiative is rolled **once per encounter, not per round**. On the not-engaged → engaged transition (`_engaged()` true: an active monster within `ENGAGE_RANGE = 5` and in `_last_visible`), `_begin_encounter` sets `_encounter_active`, rolls 1d6 per side (`_player_initiative_roll`/`_monster_initiative_roll`), and the higher side keeps the first phase (`_player_has_initiative`, ties to player) for the **whole encounter** — never rerolled while engaged. `_update_combat_camera_zoom` calls `_end_encounter` when `should_zoom` drops (disengaged or player dead), resetting the state so the next fight rolls fresh; `_regenerate` also ends any encounter so no stale initiative carries into a new dungeon. The result is logged once (compact line in the top log + the panel's pinned header built from the stored rolls), never per round.
- **Monster AI:** active only when in the player's current view. The pathfinding lives in `monster_ai.gd` (`MonsterAI.chase_step` / `flee_step` / `can_enter`); `game.gd._monster_take_turn` calls them — greedy chase, fleeing monsters move away and only attack if **cornered + adjacent**. `can_enter` consults `is_passable`, so pillars and walls are naturally avoided. Occupancy is tracked in `_monster_at` (Vector2i→Monster), kept in sync on every move/kill, and passed into the AI helpers.
- **Combat log format** (consistent for both sides): `"<Name> attacks/You attack ...: d20 R + bonus [- fatigue] = total vs AC X -> HIT|MISS"`, then on hit `"Damage: <source> NdM <roll> + bonus = total"`. HIT/MISS and morale `HOLDS`/`FLEES` are colored via BBCode (`_msg_log.bbcode_enabled = true`).
- **Fatigue** (`FATIGUE_PENALTY = 2`) subtracts from attack rolls only, shown as its own term in the calc when present.
- **Morale (2d6):** triggers (a) first time `m.hp ≤ m.max_hp/2` (`_check_half_hp_morale`, guarded by `m.morale_checked`), and (b) for each living non-fleeing monster within `MORALE_SIGHT_RANGE = 6` of a freshly-slain ally that is in `_last_visible` (`_check_ally_death_morale`, called from `_kill_monster`). Roll over the score = `FLEES` (sets `m.fleeing`).
- **Rest** (`_try_rest`) is **abstracted as `SLEEP_TURNS = 15` danger rolls**, *not* a fully simulated 15 monster turns — nothing actually moves while you sleep. Per-turn chances are tuned for cumulative risk: closed-room (`_in_closed_room`, no open doors in the room's ring) `REST_INTERRUPT_SAFE = 0.01` → ~14%, open `REST_INTERRUPT_OPEN = 0.15` → ~91%. An interrupted rest sets `fatigued` and **spawns a wandering monster** 2–5 cells from the player (`_spawn_wandering_near_player` → `DungeonPopulator.wandering_cell_near`) that "wanders in and wakes you"; an uninterrupted rest heals `1d8 + con_mod` and clears fatigue. First fatigue onset explains itself in the log (`was_fatigued` guard).
- **Battle zoom** (`_update_combat_camera_zoom`): `should_zoom = _player.hp > 0 and _engaged()`. `_set_combat_zoom(active)` early-returns if state unchanged, kills any in-flight tween, and tweens `_camera.zoom` between `CAMERA_ZOOM_NORMAL`/`CAMERA_ZOOM_COMBAT (1.12)` with `TRANS_SINE`/`EASE_OUT` over `CAMERA_ZOOM_DURATION = 0.25`. `_reset_combat_zoom()` is the instant reset used on `_regenerate`. Called from the same places that already update FOV/status after turns; never per frame.
- **Combat panel** (`combat_panel.gd`, a runtime node; built by `game.gd._setup_combat_panel`): a floating `ColorRect`+`RichTextLabel` card on `UI`, shown only while engaged. A pinned **header** rebuilt from stored initiative rolls never scrolls; below it a **current-round section** lists this turn's events (older rounds live only in the top message log). `game.gd` drives it through a narrow API: `begin_encounter`/`end_encounter`/`begin_turn` for lifecycle, and `push_attack` / `push_morale` from combat resolution (passing the raw `d20`; the panel builds the HIT/MISS card, NAT note, and 2d6 morale card internally, so the player sees *why* a monster fled). The panel relocates to the screen corner farthest from the fight — computed from the `area_provider` closure (`game.gd._combat_area_screen`) — snapping on first placement and gliding on later changes; it never jitters because it only moves when the chosen corner changes.
- **Multi-level dungeons (`_descend`/`_ascend`):** `_depth` (1 = top/entrance) is shown as `DLvl:N` on the status bar. `_levels: Dictionary` caches visited levels keyed by depth — each snapshot (`_capture_level`) holds the live `DungeonGenerator` instance, serialized monster data (`kind/cell/hp/max_hp/fleeing/morale_checked`), a deep copy of `_items_at`, and a copy of `explored_cells`. `>` on `STAIRS_DOWN` → `_try_descend` → `_descend`: snapshot current level, `_depth += 1`, then **restore** the cached level (`_restore_level`) or **generate fresh** (`_build_new_level`, which makes a *new* generator instance so cached levels aren't mutated and spawns at the new depth), arriving on the new level's up-stairs (`get_start_pos`). `<` on `STAIRS_UP` → `_try_ascend` → `_ascend`: same snapshot, `_depth -= 1`, restore the (always-cached) level above, arriving on its down-stairs (`get_down_stairs_pos`). `<` at depth 1 is refused with a message (no win/escape screen yet). Stair transitions are **instant** — they do *not* go through `_run_round` (no monster turn on arrival) — and end any encounter + reset combat zoom. `_restore_level` refills `_items_at` **in place** (`clear` + `merge(..., true)`) to preserve the renderer's by-reference link, and rebuilds monster nodes via `_add_monster` (which now **returns** the `Monster`) then overwrites the persisted fields. `_regenerate` (F5 / new game) resets `_depth = 1` and clears `_levels`, and now routes through `_build_new_level`.
- **UI gating:** input flags `_creating`, `_inventory_open`, `_awaiting_kick`/`_quaff`/`_close`/`_wield`/`_wear`, `_player_alive`, `_player.is_moving`.

### Input map

- Arrows / numpad 8·2·4·6: move. Bumping a monster = attack; closed door = open (turn-consuming); locked door = "must kick"; **pillar** = blocked with `"A stone pillar blocks the way."` (no turn, like a wall bump); wall bump silent.
- `>` / `<`: descend / ascend stairs (only on the matching stair tile, else a "no stairs" message). Accepts `KEY_GREATER`/`KEY_LESS` or shifted `KEY_PERIOD`/`KEY_COMMA`. Instant (not a round) — see the multi-level model above.
- `k` + direction: kick. If a **monster** is there: brawling attack (d20 + melee_bonus - 2, 1d4 damage, no str bonus; soft-armored player kicking a `metal_armor` monster takes the damage instead). If a **door** is there: 50% chance to bash it open. Wall = "Ouch!" `c` + direction: close door. `q`: quaff (potion picker). `w`: wield (weapon picker — now includes ranged launchers). `W` (shift+w): wear (armor/shield picker — refuses a shield while a 2H weapon is wielded, *before* spending a turn). `R`: rest/sleep.
- `f`: fire the wielded ranged weapon. Two-step prompt mirroring the pickers: it gathers inventory ammo whose `ammo_type` matches the launcher (auto-skips the ammo picker when only one type matches), then asks for a **direction**. The shot traces a straight cardinal line up to the weapon's `range`, hitting the first monster or stopping at the first opaque tile (wall/closed door/pillar). To hit = `d20 + level + dex_mod − fatigue` vs AC (`player.ranged_attack_bonus()`); damage = the **ammo's** dice (min 1, no STR). One missile is spent per shot and firing costs a turn. A **hit** consumes the missile; a **miss** has a 50% chance to leave it as a recoverable floor item, landing on the first cell that can hold it scanning back from the impact toward the player (`_do_fire_action` → `_trace_shot` / `_try_recover_ammo`). With no ranged weapon wielded, `f` just tells you to wield one; with no matching ammo it names the launcher and the ammo it needs.
- `i`: toggle the right-docked **side panel** (currently shows inventory grouped by category with equipped markers and effective stats). The panel is opaque and dev-toggled; it's not a full-screen overlay.
- Debug: **F5** new dungeon + character screen, **F6** reveal map.

## GDScript gotchas in this project

- **"Inferred Variant" warnings are treated as errors.** Typed-array `arr.back()`/`arr[i]`, `Dictionary` lookups, untyped-array elements, and any expression involving an unsafe property all return `Variant`, so results used in `var x :=` need explicit annotation (`var r: Rect2i = rooms[i]`, `var idx: int = event.keycode - KEY_A`). This is the most common parse failure here.
- **`_player` and `_monster_at` values are typed `Node2D`/`Monster` accessed loosely**, so `_player.foo` compiles even if `foo` doesn't exist and only fails at runtime. This is why renamed fields must be grepped, and why the smoke test exists. `player.gd` deliberately has no `class_name`.
- New `class_name` scripts need the editor-pass registration described above before headless runs see them.
- Renderer drawing primitives (`draw_rect`, `draw_line`, `draw_circle`, `draw_arc`) must be called inside `_draw()` — calling them from a `--script` throwaway prints "Drawing is only allowed inside NOTIFICATION_DRAW". To exercise rendering, instance the scene and `await process_frame`.
- BBCode is enabled on `MessageLog` and `StatusBar`. Single-letter square-bracket option markers like `[b]` collide with the **bold** tag — keep selection prompts in `a) ...` style, not `[a] ...`. (`_selection_prompt` does this.)
- Indentation is **tabs** (all existing scripts use tabs).
